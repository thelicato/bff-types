{
  "name": "@thelicato/bff-types",
  "version": "0.1.0",
  "description": "A generic template to provide types to both BE and FE (Best Friends Forever)",
  "license": "MIT",
  "types": "index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf docs .npmignore logo.png README.md"
  }
}
